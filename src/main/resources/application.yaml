spring:
  application:
    name: weatherapi

  data:
    redis:
      host: localhost
      port: 6379

weather:
  api:
    key: ${WEATHER_API_KEY}
    base-url: https://weather.visualcrossing.com/VisualCrossingWebServices/rest/services